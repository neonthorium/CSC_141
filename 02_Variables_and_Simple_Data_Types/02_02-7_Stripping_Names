name = " neon thorium "
message = "Names:"
print(f"{message}\n\t-{name.title()}-\n-{name.title()}-\n-{name.lstrip().title()}-\n-{name.rstrip().title()}-\n-{name.strip().title()}-")
#I've included a '-' before and after the name to show the effect of \n, \t, and the strip() methods.